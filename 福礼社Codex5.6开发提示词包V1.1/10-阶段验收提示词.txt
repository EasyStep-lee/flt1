/goal

对福礼社当前阶段做一次“只读、证据优先”的独立验收。不要修改代码、数据库、配置或外部系统；发现问题只报告，除非我另行明确授权修复。

工作目录：C:\Users\lichuanjun\Documents\flt1
方案文件：C:\Users\lichuanjun\Documents\flt1\福礼社单商户供应链平台V1.1综合方案.html
基线SHA-256（提示词生成时）：1153157234D2DCCDF38F0C5E468BD5D93889140153F1C21F7FEBB8FA5316EF92
主模型：GPT-5.6 Sol
GitHub仓库：[填写owner/repo或仓库URL；未填写时从可验证origin读取]
待验收阶段：[填写M0、M1、M2、M3、M4、M5或M6]

【验收步骤】

1. 完整读取方案、AGENTS.md、该阶段提示词、最近交接、Git状态、GitHub Issue/PR/评论、迁移、OpenAPI和测试脚本。
2. 校验方案当前SHA-256。若已变化，比较变更影响并把“基线不一致”列为前置结论。
3. 从方案提取该阶段所有P0完整验收项，不依赖开发者报告中的摘要。
4. 建立矩阵：P0编号/标题、前置数据、执行步骤、期望、自动证据、页面证据、环境、实际结果、缺口。
5. 运行本阶段focused测试、相关集成/E2E、`pnpm verify`、`pnpm prisma:migrate:dry-run`、`pnpm openapi:generate`、`pnpm openapi:check`；记录命令、退出码和关键输出。
6. 对高风险不变量做独立检查：越权/数据隔离、金额与资金守恒、库存并发、幂等重放、状态机跳步、隐私字段、迁移与回滚。
7. 页面验收须核对真实入口、路由、可操作状态及加载/空态/失败/无权限/弱网；仅存在源文件或截图路径不算通过。
8. 用GitHub连接器读取PR/Issue结构化信息，用`gh`读取实际Actions运行和失败日志；确认每个CI结论对应PR最新提交，旧运行不得复用。
9. 外部能力分别标记LOCAL_PASS、CI_PASS、STAGING_PASS、DEVICE_PASS、PRODUCTION_PASS、NOT_EXECUTED、BLOCKED或FAIL。

【关键判定规则】

- 只要一个阶段阻塞P0失败，阶段结论就是FAIL或BLOCKED，不能写“基本通过”。
- 自动测试通过不能证明微信真机支付、导航、拨号、合法域名、备案或正式发布通过。
- 旧日志、旧截图、旧CI和开发者自述只能作为线索，不算本轮新鲜证据。
- 不得为了让验收变绿而改测试、降级断言、跳过命令或改变方案口径。
- 如果测试本身只做文本锚点、路径存在或快照占位，标为“证据不足”。
- PR已创建、旧提交CI通过或评论已回复都不等于阶段通过；PR最新提交CI、人工合并及`main`最新CI任一未通过，或仍有未解决P0/P1时，不得给阶段PASS。
- 本次只读验收不得修改PR、关闭Issue、推送修复或合并；发现问题只给最小修复顺序。

【阶段专项抽查】

- M0：干净安装、版本锁定、开发/测试/构建、迁移dry-run、确定性OpenAPI/统一类型/Web与小程序传输适配、Docker依赖、CI。
- M1：公司/供应商主体、职能独立页面、自然人双人复核、路由守卫、supplierId隔离、会话失效、审计。
- M2：分类模板、双审核、三价格、供应价泄露、集采标识、共用库存、“看他还卖什么”。
- M3：跨供应商聚合、库存原子预扣、福利卡账本、永久无个人充值、微信幂等、混合支付、原结构退款、企业采购私有缓存/noindex边界。
- M4：个人/企业链路隔离、任务幂等、并发抢单、节点、转派失权、取送双导航、隐私。
- M5：售后归因、账单快照、差异、线下结算、资金核对、门户服务端HTML/ISR/SEO/缓存隔离、CMS、导出。
- M6：P0-1至P0-119、staging、真机、安全、性能、备份恢复、监控、发布和回滚。

【输出格式】

1. 验收结论：PASS / FAIL / BLOCKED。
2. 阻塞问题：按P0/P1/P2排序，给出文件/接口/页面/测试及可复现证据。
3. P0矩阵：逐项结果，不合并省略。
4. 命令证据：环境、提交、命令、退出码、关键结果。
5. 环境边界：哪些只在本地，哪些已在CI、staging、真机或生产验证。
6. 风险与建议：最小修复顺序，但本次不实施。
7. GitHub证据：仓库、阶段Issue/PR、分支、提交SHA、Draft/Ready、最新Actions及对应提交、未解决评论、人工合并和`main`复验状态。
