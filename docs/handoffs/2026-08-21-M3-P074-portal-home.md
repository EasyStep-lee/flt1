# M3-P074 门户首页闭环交接

## 结论

- 阶段：`M3_IN_PROGRESS`；本切片：`M3-P074 / P0-074`；M3 不因本切片自动 PASS。
- 当前证据：`CI_PASS`；PR head `ea1f19673a532c4bbd6e9354c5b381ab087c46c7` 的完整门禁已通过，仍等待最终证据 head CI、人工合并和 post-merge `main` CI。
- 方案 SHA-256：`1153157234D2DCCDF38F0C5E468BD5D93889140153F1C21F7FEBB8FA5316EF92`。
- 基线：`origin/main@099606b97d86653c37825ac5e25ea2e1eaa6077c`；P073 PR #119 精确 head `1abef37c40f36d45f0ecd469a33b6aecee4de754` 已合并，post-merge main run `32449212006` / job `96674355395` 成功。
- 分支：`codex/m3-portal-home`；Issue #120；Draft PR #121；首轮精确 head CI run `32460128787` / job `96705296904` 成功。
- 用户已有 M3-P031、M3-P051、M3-P059 截图改动及其他无关未跟踪文件保持原样，未纳入本切片。

## 实际变更

- 首页按主视觉、核心服务、供应链能力、社区集采、分类预览、经授权案例、供应商合作、新闻公告、底部企业服务 CTA 九段固定顺序重组。
- 分类预览只展示 EXT-007 已批准的食品、家居日用、个护、纸品、家庭清洁、文体办公六个一级分类，不展示商品、价格或精确库存。
- 当前没有可验证的客户案例公开授权，页面显示真实空态；匿名能力场景明确标注“不是客户案例”。
- 新闻只使用既有版本化服务边界公告，不复制高保真视觉稿中的虚构公司动态、日期或商品价格。
- P0-001 唯一经营主体和 P0-009 无供应商店铺证据嵌入九段首页后继续通过。
- 无 Prisma 迁移、OpenAPI、DTO、错误码、CMS 或真实登录实现。

## 先失败后通过证据

| 证据 | 结果 |
|---|---|
| RED P0-074 Chromium | `FAIL 2/2`：首页无九段行为标记；移动端无法定位目标段 |
| 首次 GREEN 尝试 | 测试启动器读取旧 `.next` 产物，仍 `FAIL 2/2`；未修改断言，先重建真实 Next.js 产物 |
| Portal lint/typecheck/build | PASS；首页为静态 ISR 5 分钟，私有企业交易路由保持动态 |
| GREEN P0-074 Chromium | `PASS 2/2` |
| Portal tests | `PASS 4/4` |
| P0-001/009/027/030/073/074 回归 | Chromium `PASS 18/18` |
| 完整 `pnpm verify` | `PASS 17/17`；API `247/247`，P0 E2E `89/89`，迁移演练 `empty=2 / upgrade=2 / restore=2 / product=37 / cleanup=PASS` |
| PR CI（head `ea1f196…`） | run `32460128787` / job `96705296904` `PASS`；`PNPM_VERIFY_OK:steps=17`，P0 E2E `89/89`，秘密扫描 `1105` 个跟踪文件 |
| 证据 head `f7773ae…` CI | run `32461223687` / job `96708384546` `FAIL`；回归契约拒绝复合状态值 `CI_PASS_RUN_...`，门户行为未失败；已改回标准枚举 `CI_PASS`，等待新 head CI |
| CI 状态修复 focused 回归 | 首次 `pnpm test` 暴露 7 个只接受早期专用 CI 标记的历史断言；最小扩展为接受标准 `CI_PASS` 后，全部 contracts `PASS 95/95`、handoff 目标测试 `PASS 3/3`、P074/冻结 `PASS 8/8` |

## P0、环境与外部边界

- P0-074 当前 `CI_PASS`；证据来自 PR head `ea1f19673a532c4bbd6e9354c5b381ab087c46c7` 的 run `32460128787` / job `96705296904`。
- 本地环境：Windows，Node `22.23.1`，pnpm `10.12.1`，Next.js `16.2.12` production build，Playwright Chromium。
- 本切片无迁移和 OpenAPI 变化；完整 `pnpm verify` 已重跑迁移演练、确定性 OpenAPI 生成/差异/破坏性检查并通过。
- 客户名称、Logo、图片和数据公开授权：`NOT_EXECUTED`；当前安全空态不是授权案例完成证据。
- staging、真实域名/备案、device、production：`NOT_EXECUTED`。

## 风险与回滚

- 高保真视觉稿含示意商品、价格、场景和新闻，本实现只复用布局层级，不把示意内容写入正式页面。
- M5 CMS 接入后必须继续执行授权案例默认拒绝和公开 DTO 白名单；本切片不提前建设 CMS。
- 无数据迁移或 API breaking change；回滚只需撤销首页、公共内容常量、样式、测试和证据文件。

## GitHub 与下一门禁

- 仓库：`EasyStep-lee/flt1`；基线分支：`main`；开发分支：`codex/m3-portal-home`；Issue #120；Draft PR #121。
- 证据更新提交形成最终 head 后必须再次通过 Actions；未经用户对该最终 head 明确授权，不得转 Ready 或合并。
- `M3-P075` 保持锁定。只有 P074 PR 合并且合并后 `main` CI 成功，才允许进入 P075；M4 及以后继续锁定。
