# M0-002 本机与仓库环境只读盘点交接

## 1. 身份

- 阶段/任务：`M0 / M0-002 只读盘点本机与仓库环境`
- 盘点时间/时区：2026-08-02 05:12，UTC-04:00
- 本地仓库：`C:\Users\lichuanjun\Documents\flt1`
- 开发分支：`codex/m0-m0-002`
- 盘点基准HEAD：`1400599215e9e70b4294ad6c4cd810ae2518cae7`
- 实现提交：`96346b15c23debfff03cf8c328da51e26bc3669c`
- 操作方式：`READ_ONLY`；除生成本任务报告和测试证据外，未安装软件、未启动服务、未修改系统配置

## 2. 结论

M0-002完成，结论为 `M0_002_PASS_WITH_GAPS`。本机已经具备Node 22、pnpm 10、Git、GitHub CLI、Docker CLI/Compose和微信开发者工具；Docker后台当前不可用，本机未发现MySQL/Redis命令。上述缺口不阻断M0-002本身，由后续M0-005按任务边界处理。

唯一外部阻塞是目标GitHub仓库尚未确认：本地没有 `origin`，虽然GitHub CLI已认证 `EasyStep-lee`，但不能据此猜测仓库或获得远程写入授权。唯一允许继续的任务为 `M0-003`。

## 3. 已确认事实

| 项目 | 只读结果 | 后续影响 |
|---|---|---|
| 操作系统 | Windows 11 专业版，10.0.26200 build 26200，x64；PowerShell 7.6.3 | M0 |
| 磁盘 | C盘可用约273.06 GiB | M0 |
| Node.js | `v22.23.1` | 满足M0-004目标主版本 |
| npm / pnpm / Corepack | `10.9.8` / `10.12.1` / `0.34.6` | pnpm满足M0-004目标主版本 |
| Git / GitHub CLI | Git `2.53.0.windows.2`；gh `2.92.0`；账号 `EasyStep-lee` 已认证 | M0-003仍需确认目标仓库和权限 |
| Docker | CLI `29.6.2`；Compose `v5.3.1` | 客户端存在，后台不可用 |
| 微信开发者工具 | 已发现 `2.01.2510260` 和 `cli.bat` | 后续小程序编译/真机仍需独立验收 |
| 计划端口 | 3000—3005、3306、6379当前没有监听进程 | M0-004/M0-005可继续规划 |
| 敏感环境变量 | 仅检查名称是否存在；7个计划变量均未在当前进程环境中发现，报告不保存任何值 | 后续使用模板和秘密管理，不写入Git |
| 工作树 | 盘点时存在14项未跟踪条目；本任务没有删除或覆盖，提交必须精确暂存 | 所有后续任务 |

## 4. 缺口

| 代码 | 事实边界 | 归属任务 |
|---|---|---|
| `DOCKER_DAEMON` | Docker Desktop Service为Stopped/Manual，daemon不可读；本任务没有启动它 | M0-005 |
| `MYSQL_CLIENT` | PATH中未发现 `mysql` / `mysqld`；不代表后续Docker服务不可用 | M0-005 |
| `REDIS_CLIENT` | PATH中未发现 `redis-server` / `redis-cli`；不代表后续Docker服务不可用 | M0-005 |

这些是已盘点缺口，不是M0-002失败，也不能被表述为数据库或缓存已经可用。

## 5. 外部阻塞

| 代码 | 当前证据 | 禁止事项 | 解除条件 |
|---|---|---|---|
| `GITHUB_REPOSITORY_UNCONFIRMED` | `git remote -v`为空 | 猜测owner/repo、添加未知origin、推送、创建PR或声称CI已运行 | M0-003确认目标仓库、远程地址和写入权限 |

远程/PR/CI状态统一为 `BLOCKED_EXTERNAL`；本任务没有任何远程写入。

## 6. 验证证据

| 验证 | 命令/证据 | 结果 |
|---|---|---|
| 先红 | `./tests/environment/development-environment.inventory.ps1` | 预期失败：缺少盘点脚本 |
| 数量回归先红 | 同一测试增加未跟踪数量断言 | 预期失败：中文插值导致数量缺失 |
| focused test | `./tests/environment/development-environment.inventory.ps1` | PASS：结构完整、数量可复核、敏感值未写入 |
| 实际盘点 | `./scripts/inventory-development-environment.ps1` | PASS：6项确认事实、3项缺口、1项外部阻塞 |
| 机器可读证据 | `artifacts/verification/M0-002/environment-inventory.json` | `schemaVersion=1.0.0`、`mode=READ_ONLY`、下一任务M0-003 |
| 产品基线守卫 | `./tests/baseline/product-baseline.guard.ps1` 与 `./scripts/verify-product-baseline.ps1` | PASS；执行包状态变化产生预期快照告警，方案/提示词/UI冻结基线未漂移 |
| 执行包自检 | `福礼社Codex5.6开发执行包V1.1/scripts/verify-execution-pack.ps1` | PASS：149任务、119 P0、658字段、80页面、22权限 |
| 总控工作簿 | 重新生成并渲染12个工作表，扫描公式错误 | PASS：0个公式错误；SHA-256 `16EEF7CA739E8C5F3AEAECDF941339181449E570699567F6C699D941EE22B0AA` |

- `pnpm verify`：`NOT_EXECUTED`。工程脚本尚未由M0-004/M0-011建立，不能用空脚本冒充通过。
- Docker/MySQL/Redis连通性：`NOT_EXECUTED`，归属M0-005。
- 微信开发者工具登录、编译、真机：`NOT_EXECUTED`，不以安装发现代替验收。
- 预发布、生产、真实支付：`NOT_EXECUTED`。
- 项目内执行包与桌面执行包副本均已通过同一自检；M0-002为 `DONE/LOCAL_PASS`，M0-003为唯一 `READY` 任务。

## 7. 本任务文件

- `scripts/inventory-development-environment.ps1`
- `tests/environment/development-environment.inventory.ps1`
- `artifacts/verification/M0-002/environment-inventory.json`
- `docs/handoffs/2026-08-02-M0-002-environment.md`

## 8. 下一任务

- 唯一允许开始：`M0-003 确认或创建GitHub仓库并建立远程基线`。
- M0-003必须先核实仓库owner/repo、可见性、origin地址和写权限；无法从本机证据确认时必须停在外部阻塞，不得猜测。
- 禁止提前执行M0-004及以后、任何业务开发、远程写入、真实支付或生产操作。
- M0阶段门禁：`NOT_EXECUTED`。
