# 2026-08-13 M3-P020 用户首页交接

阶段结论：`IN_PROGRESS / LOCAL_PASS`。M3-000 已由 PR #76 合并到 `main@05c87e058d86ab05dfaa09b6bb6d4341e7f45019`，合并后 Actions run `31697763759` 成功。本切片完成个人小程序首页、固定四 TabBar 与公开零售货架 API 的本地行为验证；实现提交 `0f8257ca79ec6feb6893f45747c3bda4894cbd55` 已通过完整门禁，尚未取得 Draft PR 精确 head CI，微信开发者工具、真机、staging 与 production 均未执行。

## 范围与基线

- 方案 SHA-256：`1153157234D2DCCDF38F0C5E468BD5D93889140153F1C21F7FEBB8FA5316EF92`。
- 当前任务：M3-P020；P0-020；PAGE-049；API-029。
- 分支：`codex/m3-user-home`；Issue #77。
- 非目标：分类/搜索/活动/福利卡账户/登录/购物车/订单/支付/库存预扣/配送/企业采购；无迁移。

## 实际实现

- API 新增公开 `GET /v1/catalog/products`，按 ACTIVE 公司/供应商/商品、ENABLED 分类、零售启用、ACTIVE SKU 和监管条件查询统一货架。
- 显式 DTO 只返回个人零售所需字段；供应价、企业价、库存与内部归属字段不得进入响应。
- 小程序首页通过生成 OpenAPI 类型和 `miniapp-kit` 适配器加载货架，提供 loading/empty/error/retry/success 状态。
- 固定 TabBar 为首页、分类、购物车、我的；搜索、活动、福利卡、配送区域和个人订单只作为页面入口，不占 TabBar，也没有企业入口。
- 分类、购物车、我的仅提供诚实的阶段占位页，未将对应后续 P0 标为完成。

## RED 与 GREEN 证据

- RED：API 行为测试 3/3 因 `/v1/catalog/products` 为 404 失败；小程序行为测试 3/3 因首页构建产物缺失、首页非首路由且无四 TabBar 失败。
- GREEN：API 3/3、小程序 3/3、Prisma 仓储 5/5、P0 Playwright 1/1 通过。
- 全量 `pnpm verify`：实现提交 `0f8257c` 于 2026-08-13T13:38:29.999Z 至 13:50:08.833Z 执行，17/17 通过；其中 P0 E2E 52/52，通过报告为 `artifacts/test-results/verification/pnpm-verify.json`。

## 安全与不变量

- 唯一销售、结账与售后主体保持江苏福礼团供应链科技有限公司；供应商不是店铺。
- 个人小程序响应只含零售价（整数分），无供应价、企业价、毛利、应付、企业或买家作用域字段。
- 未选配送区为诚实的 `UNSELECTED`；在没有服务端验证会话前拒绝客户端伪造 `regionCode`。
- 不新增个人充值、支付、订单、库存写入、外部副作用或秘密配置。

## 环境边界、风险与回滚

- `LOCAL_PASS` 仅为 Windows/Node 22、本地 mock 外部依赖；不是 CI、staging、真机或生产证据。
- 参考图有五项底部导航，但综合方案锁定四 TabBar；实现以方案为准。
- 回滚：回退本切片应用/API/契约/文档提交即可；无数据库迁移和数据回滚。

## 下一门禁

提交本次证据回填、推送并创建 Draft PR，读取精确 head Actions 和未解决评论。只有 PR 经人工精确 head 授权合并且合并后 main CI 成功，才能开始 M3-P022。当前明确禁止订单、库存预扣、福利卡、支付、M4、M5、M6 和真实外部操作。
