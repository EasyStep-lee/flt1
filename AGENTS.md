# 福礼社 V1.1 仓库执行规则

本仓库只能以 `福礼社单商户供应链平台V1.1综合方案.html` 为产品基线。开始任何任务前必须读取：

1. 本文件；
2. `福礼社Codex5.6开发提示词包V1.1/AGENTS.md`；
3. `docs/product/baseline-lock.json`；
4. 当前任务、阶段门禁和最近交接记录。

方案预期 SHA-256 为 `1153157234D2DCCDF38F0C5E468BD5D93889140153F1C21F7FEBB8FA5316EF92`。先运行：

```powershell
./scripts/verify-product-baseline.ps1
```

校验失败时必须停止受影响开发并执行基线变更流程，不得用 V1.0、0801、V29 或更早方案覆盖 V1.1。

## 不可重新设计的边界

- 江苏福礼团供应链科技有限公司是唯一对客交易、收款、开票、退款和售后主体；供应商不是店铺且不直接收款。
- 个人与企业均可跨供应商统一向公司结账；企业统一配送不进入个人跑腿大厅。
- 上架后供应价变更须审核；平台零售价和集采销售价免审生效并追加留痕。
- 供应价只对公司及对应供应商授权价格/财务职能可见，对客 DTO 永不返回。
- 现金线上支付只保留微信；福利卡无个人现金充值，资金来源只允许企业发放、公司赠送、实体卡或卡密兑换。
- 双人复核按自然人 `identityType + identityId` 隔离，同一人跨职能账号不得自审，超级管理员不得绕过。
- 门户公开内容采用 Next.js SSG/ISR；登录、预览和交易区域必须 `noindex` 且禁止公共缓存。
- OpenAPI 由后端确定性生成；Web 使用 `openapi-fetch`，原生小程序经 `miniapp-kit` 的 `wx.request` 适配器复用类型。

## 执行纪律

- 严格按 M0 至 M6 和 `nextAllowedTask` 顺序推进；一次仅一个任务为 `IN_PROGRESS`。
- 先写失败测试，再最小实现，再运行 focused tests；没有真实证据不得写成通过。
- 不覆盖用户未提交文件，不使用 `git reset --hard` 或无审查的 `git add -A`。
- 未确认 GitHub `owner/repo` 或可验证 `origin` 前只做本地工作，不猜测远程。
- 真实支付、退款、银行转账、法务、真机、生产迁移和上线由授权人工执行。

完整角色、工程、测试和 GitHub 规则以 `福礼社Codex5.6开发提示词包V1.1/AGENTS.md` 为准；若两者出现冲突，立即停止并按基线变更流程同步修订，不得自行选择更宽松口径。
