import { requestAdapter } from '../../request-adapter.js';

void requestAdapter;

Page({
  data: {
    boundary: '独立跑腿员会话与唯一miniapp-kit请求适配边界',
    shellId: 'runner-miniapp-shell',
    title: '福礼社跑腿小程序应用壳',
  },
});
