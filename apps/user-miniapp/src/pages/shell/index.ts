import { requestAdapter } from '../../request-adapter.js';

void requestAdapter;

Page({
  data: {
    boundary: '独立用户会话与唯一miniapp-kit请求适配边界',
    shellId: 'user-miniapp-shell',
    title: '福礼社用户小程序应用壳',
  },
});
